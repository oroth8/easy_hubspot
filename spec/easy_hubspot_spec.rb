# frozen_string_literal: true

RSpec.describe EasyHubspot do
  it "has a version number" do
    expect(EasyHubspot::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
